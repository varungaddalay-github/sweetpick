# Core FastAPI dependencies for Vercel
fastapi==0.109.2
uvicorn==0.27.1
pydantic==2.6.1
pydantic-settings==2.1.0
python-dotenv==1.0.1

# Data collection - keep essential only
google-search-results==2.4.2
requests==2.31.0
aiohttp==3.9.3
urllib3<2.0.0

# AI/ML
openai==1.12.0
numpy==1.26.4

# Vector database - optimized version
pymilvus>=2.3.0,<3.0.0

# Essential utilities only
python-multipart==0.0.9
httpx==0.26.0
tenacity==8.2.3
loguru==0.7.2

# Remove heavy dependencies for serverless:
# - pandas (too large for serverless)
# - psycopg2-binary (not needed if using managed DB)
# - redis (can be optional)
# - testing libraries (not needed in production)
# - development tools (not needed in production)
